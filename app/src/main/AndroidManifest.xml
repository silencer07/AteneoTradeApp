<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tradeapp.ateneo.edu.tradeapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".TradeApp_"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity_"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProductListActivity_" />
        <activity android:name=".ProductDetailsActivity_" />
        <activity
            android:name=".LoginActivity_"
            android:label="@string/title_activity_login" />
        <activity android:name=".AccountDetailsActivity_" />
        <activity android:name=".AddProductActivity_" />
        <activity android:name=".MessageActivity_"></activity>
        <activity android:name=".AccountFeedbackActivity_"></activity>
    </application>

</manifest>